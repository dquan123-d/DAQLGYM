@model List<DAQLGYM.Models.TblMenu>

@foreach (var item in Model.Where(i => i.Levels == 1))
{
    <li class="drop-holder">
        <a helf="/@item.Alias">
            @item.Title
        </a>
    </li>

}
